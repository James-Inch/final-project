import React, { Component } from "react";
import Scraped from "../../components/Scraped";

class Home extends Component {
    render() {
        return (
            <Scraped />
        );
    };
};

export default Home;